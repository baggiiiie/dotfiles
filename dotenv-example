# .env file (sourced on every cd)
GH_HOST=github.com

# .env-work file (sourced when in ~/Desktop/repos/work)
JENKINS_TOKEN=token

# .env-jira file (sourced at shell initialization)
JIRA_API_TOKEN=your_jira_token_here
JIRA_AUTH_TYPE=bearer

